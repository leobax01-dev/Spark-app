function createDisposable(dispose) {
    let active = true;
    let result;
    return {
        dispose: ()=>{
            if (active) {
                active = false;
                result = dispose();
            }
            return result;
        }
    };
}
export { createDisposable };
